# 产品决策记录

面试前把本文件过一遍。数字来自样本知识库上的设计目标，上线后用 `test-questions.md` 实测定稿。

## 决策 1：为什么 RAG 不微调？

```
知识：内部文档每周更新（退款政策、价格、权限都会变）
结论：RAG 增量入库即可；微调重训成本高、知识过期后幻觉更危险
```

DocFlow 的核心价值是「可追溯的内部事实」，不是「更会说话的模型」。模型只负责组织语言，事实以知识库切片为准。

## 决策 2：Chunk 策略


| 尝试                         | 结果                 | 最终            |
| -------------------------- | ------------------ | ------------- |
| 1000 token                 | 召回模糊，引用卡片里夹杂无关段落   | —             |
| 500 token + 100 overlap    | 引用边界更清晰，同义改写仍能命中   | ✅ 采用          |
| 按标题切 Markdown（分隔符 `\n##` ） | 结构清晰，适合 FAQ / 制度文档 | ✅ Markdown 优先 |


实现位置：`lib/env.ts` 的 `getChatProcessRule()`，上传文档时带给 Dify。

## 决策 3：Top-K 与兜底

- Dify 知识库检索 Top-K = 5，再交给生成。
- 前端拒答阈值：最高相似度 `< 0.6` → 展示灰色拒答卡。
- Prompt 约束：「仅基于引用回答，无依据说不知道」。文案见 `samples/dify-prompt.md`。
- 即使模型写了一段话，只要命中拒答句式或空检索，UI 仍走 `RefusalState`，避免「看起来像答案」。

阈值写在 `REFUSAL_SCORE_THRESHOLD`，默认 0.6。调参时只改环境变量，不改产品语义。

## 决策 4：交互设计


| 状态   | UI                     | 为什么                      |
| ---- | ---------------------- | ------------------------ |
| 检索中  | 「正在查阅知识库文档…」+ Skeleton | 降低等待焦虑，让用户知道系统在查文档而不是在瞎编 |
| 流式生成 | 打字机 + 可随时停止            | 长回答可控；SSE 状态机面试可讲        |
| 有引用  | 可展开原文片段，展示相似度          | 信任感；PM 可用它讲「引用准确率」       |
| 拒答   | 灰色卡片 + 换问法建议           | 比胡说强，对应幻觉率指标             |
| 出错   | 错误码人话化 + 重试            | 本地 Dify 挂了也能恢复           |
| 反馈   | 👍/👎 写入 localStorage  | 为 Evals 故事埋伏笔，不必先上用户系统   |




## 决策 5：为什么用 Dify 而不是自建向量库？

求职阶段要在 3 周内做出可演示原型。自建 pgvector 会把时间花在 embedding 管道，而不是产品决策。Dify 负责检索编排，本仓库负责：

- API Key 不暴露给浏览器（Next.js Route Handler 代理）
- SSE 状态机（retrieving → streaming → done / refused / error）
- 引用、拒答、反馈这些「AI 产品交互」

Service API 默认 `http://127.0.0.1/v1`。

## 决策 6：为什么不做多 Agent / 微调 / 账号体系？


| 不做         | 产品理由                         |
| ---------- | ---------------------------- |
| 多 Agent 编排 | 场景是问答不是自动化，Chatflow / 聊天助手够用 |
| 微调         | 知识会变，RAG 更合适                 |
| 用户系统/权限    | MVP 验证核心价值，二期再加              |
| 语音/多模态     | 超出范围，避免 scope creep          |




## 评测复盘（关键词 → nomic → bge）

用 `test-questions.md` 20 题对照了三套检索。结论：失败要先拆成「没召回」还是「召回了但生成套错」，不能一律怪模型。

### 怎么拆

对每道错题同时看：Top-5 切片、分数、生成原文。金标所在 chunk 不在 Top-5 → **检索失败**；在 Top-5 但数字/流程错 → **生成失败**。

### 两条典型错题

**差价怎么算（生成失败）**  
`03` 已按 `##` 切出完整「差价政策」（公式 + 90 天手续费 + 不退差价）。bge 下这段在 Top-5，但「产品定义」（9800 / 19800）分更高。1.5B 先做目录价相减得到 10000，再把「免手续费，只补差价」改写成自相矛盾的「不需要支付补差价」。切片对、排序偏、小模型分不清「目录价差额」和「按天折算的补差价」。

**账号开通要多久（检索失败）**  
时效写在「常规研发权限：工作日 4 小时」；问句对齐的是另一块「账号开通清单」（只有开通项目 + 文末离职回收 2 小时）。4 小时段未进 Top-5；「多久」还把报销/退款周期挤进来。关键词曾答成 2 小时（套用回收 SLA），Embedding 曾答成 1–2 天或入职五步。根因是切块标题和用户口吻错位，模型只是在错误上下文里找了个数字或步骤。

### Embedding 对照（同一知识库）


|       | jieba 关键词         | nomic-embed-text       | bge-small-zh          |
| ----- | ----------------- | ---------------------- | --------------------- |
| 特点    | 字面对上就中            | 偏英文，短中文问句容易塌到泛「流程」块    | 中文小模型，25MB / 512 维    |
| 怎么退货  | 召回 `01` 但编成通用电商退货 | 「退货流程」相似度 0.55，排不上，误拒答 | 退货流程排第 1（约 0.66），五步正确 |
| 数字码退款 | 能答不能退             | 同样误拒答                  | 命中「不支持退款」，答不可以        |
| 年假    | 空召回后幻觉五步审批        | 语义命中 `04`（约 0.81）      | 与问句标题对齐，稳定            |
| 代价    | 无向量               | 768 维，中文短问不稳           | 切模型必须重建索引（维度不同）       |


第 9 题年假能中，是因为正文已写成用户口吻「年假有几天」。这是内容侧补丁，不能当成生产检索的全部解法。

### 演示阶段做了什么

- 切块：`separator: "\n## "`，500 token，overlap 100（见决策 2）。
- 提示词：只根据片段答、多问都要答、无依据说「文档中未提及」（`samples/dify-prompt.md`）。
- 前端：最高分 `< 0.6` 或拒答句式 → 灰色拒答卡，不展示编造正文。
- 本机 8GB：对话 `qwen2.5:1.5b`，向量从 nomic 换成 `bge-small-zh`。

这些能讲清假设、能量化 20 题，但解决不了「答案在邻段、标题对不上问句」。

### 生产解法（按层，不混在一起改）

1. **切块**：parent-child。子块用来搜，命中后把父段（整篇「权限与账号」）送进模型，邻段 4 小时能被带上。
2. **查询**：问「多久」就改写成「SLA / 时效 / 工作日」；给切片打 `action=provision|offboard`、`metric=duration`，过滤离职回收和报销周期。
3. **检索**：混合检索（向量 + 关键词）召回 Top-20，rerank 压到 3–5。关键词能保住「开通」「4 小时」，向量负责同义。
4. **生成**：问时长只许采用主语为「开通」且带小时/工作日的句子；回复中的数字必须在引用 span 里出现（「1–2 天」会被丢掉）。关键政策可先抽 `{开通SLA, 回收SLA}` 再组织句子。
5. **评测**：召回（金标 chunk 是否在 Top-5）、数字正确、该拒答是否拒答，三套指标分开看。差价记生成失败，开通记检索失败，工单派给不同改动。

文档仍要规范（问法写进正文、冲突数字不要只出现在标题无关的段末），这是降检索难度，不能代替上面管道。

面试收口：选 RAG 是因为制度周更；演示用 20 题证明「拒答优于胡说」，并用错题说明检索失败 ≠ 生成失败。二期是 parent-child、混合检索+重排、数字接地校验，而不是上多 Agent。

## 指标口径（面试用）


| 指标         | MVP 目标 | 怎么测                                  |
| ---------- | ------ | ------------------------------------ |
| 答案有用率      | ≥ 75%  | 20 题人工标注，见 `test-questions.md` `85%` |
| 引用准确       | ≥ 80%  | 回答中的引用能在文档中找到100%                   |
| 幻觉率        | ≤ 15%  | 无依据断言 / 总回答数 0                       |
| 拒答合理       | 100%   | 「文档未提及」类问题必须拒答 100%                  |
| 首 Token 延迟 | < 2s   | 前端 `performance.now()`，气泡下方展示        |


